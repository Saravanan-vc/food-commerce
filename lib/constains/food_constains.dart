import 'package:food/widgets/star_widgets.dart';

const List<String> foodList = ["all", "Hot Dog", "Bruger", "Pizza", "KFC"];
List starList = List.generate(5, (index) => StarWidgets.starcontainer());
